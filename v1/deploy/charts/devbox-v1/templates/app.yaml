{{- if and .Values.frontend.enabled .Values.app.enabled }}
apiVersion: app.sealos.io/v1
kind: App
metadata:
  name: {{ .Values.app.name | quote }}
  namespace: {{ .Values.app.namespace | quote }}
spec:
  data:
    desc: Devbox
    url: {{ include "devbox-v1.frontendExternalURL" . | quote }}
  icon: {{ printf "%s/logo.svg" (include "devbox-v1.frontendExternalURL" .) | quote }}
  i18n:
    zh:
      name: Devbox
    zh-Hans:
      name: Devbox
  name: {{ .Values.app.name | quote }}
  type: {{ .Values.app.type | quote }}
  displayType: {{ .Values.app.displayType | quote }}
{{- end }}
