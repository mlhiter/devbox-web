{{- if and .Values.frontend.enabled .Values.ingress.enabled }}
{{- $externalPortSuffix := include "devbox-v2-frontend.externalPortSuffix" . }}
{{- $rootExternalOrigin := include "devbox-v2-frontend.rootExternalOrigin" . }}
{{- $wildcardExternalOrigin := include "devbox-v2-frontend.wildcardExternalOrigin" . }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: {{ .Values.ingress.className | quote }}
    nginx.ingress.kubernetes.io/configuration-snippet: |
      more_clear_headers "X-Frame-Options:";
      more_set_headers "Content-Security-Policy: default-src * blob: data: *.{{ .Values.cloudDomain }}{{ $externalPortSuffix }} {{ .Values.cloudDomain }}{{ $externalPortSuffix }}; img-src * data: blob: resource: *.{{ .Values.cloudDomain }}{{ $externalPortSuffix }} {{ .Values.cloudDomain }}{{ $externalPortSuffix }}; connect-src * ws: wss: blob: resource:; style-src 'self' 'unsafe-inline' blob: *.{{ .Values.cloudDomain }}{{ $externalPortSuffix }} {{ .Values.cloudDomain }}{{ $externalPortSuffix }} resource:; script-src 'self' 'unsafe-inline' 'unsafe-eval' blob: *.{{ .Values.cloudDomain }}{{ $externalPortSuffix }} {{ .Values.cloudDomain }}{{ $externalPortSuffix }} resource: *.baidu.com *.bdstatic.com; frame-src 'self' {{ .Values.cloudDomain }}{{ $externalPortSuffix }} mailto: tel: weixin: mtt: *.baidu.com; frame-ancestors 'self' {{ $rootExternalOrigin }} {{ $wildcardExternalOrigin }}";
      more_set_headers "X-Xss-Protection: 1; mode=block";
    higress.io/response-header-control-remove: X-Frame-Options
    higress.io/response-header-control-update: |
      Content-Security-Policy "default-src * blob: data: *.{{ .Values.cloudDomain }}{{ $externalPortSuffix }} {{ .Values.cloudDomain }}{{ $externalPortSuffix }}; img-src * data: blob: resource: *.{{ .Values.cloudDomain }}{{ $externalPortSuffix }} {{ .Values.cloudDomain }}{{ $externalPortSuffix }}; connect-src * ws: wss: blob: resource:; style-src 'self' 'unsafe-inline' blob: *.{{ .Values.cloudDomain }}{{ $externalPortSuffix }} {{ .Values.cloudDomain }}{{ $externalPortSuffix }} resource:; script-src 'self' 'unsafe-inline' 'unsafe-eval' blob: *.{{ .Values.cloudDomain }}{{ $externalPortSuffix }} {{ .Values.cloudDomain }}{{ $externalPortSuffix }} resource: *.baidu.com *.bdstatic.com; frame-src 'self' {{ .Values.cloudDomain }}{{ $externalPortSuffix }} mailto: tel: weixin: mtt: *.baidu.com; frame-ancestors 'self' {{ $rootExternalOrigin }} {{ $wildcardExternalOrigin }}"
      X-Xss-Protection "1; mode=block"
  name: devbox-frontend
  namespace: {{ .Values.frontend.namespace | quote }}
spec:
  rules:
  - host: {{ include "devbox-v2-frontend.frontendHost" . | quote }}
    http:
      paths:
      - pathType: Prefix
        path: /
        backend:
          service:
            name: devbox-frontend
            port:
              number: 3000
  {{- if not (eq (toString .Values.disableHttps) "true") }}
  tls:
  - hosts:
    - {{ include "devbox-v2-frontend.frontendHost" . | quote }}
    secretName: {{ .Values.certSecretName | quote }}
  {{- end }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: {{ .Values.ingress.className | quote }}
  name: devbox-challenge
  namespace: {{ .Values.frontend.namespace | quote }}
spec:
  rules:
  - http:
      paths:
      - backend:
          service:
            name: devbox-frontend
            port:
              number: 3000
        path: /api/.well-known/devbox-domain-challenge
        pathType: Prefix
{{- end }}
